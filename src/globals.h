#ifndef GLOBALS_H
#define GLOBALS_H

extern const int SCREEN_WIDTH = 256;
extern const int SCREEN_HEIGHT = 240;

extern SDL_Window* gWindow = NULL;
extern SDL_Renderer* gRenderer = NULL;

#endif // GLOBALS_H
